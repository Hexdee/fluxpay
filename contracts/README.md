# Contracts

Hardhat project for the FluxPay `PaymentProcessor` contract used by the hosted checkout.

## Setup

```bash
npm install --prefix contracts
cp contracts/.env.example contracts/.env
```

## Compile

```bash
npm run build --prefix contracts
```

## Deploy (example)

```bash
npm run deploy:espace --prefix contracts
```

